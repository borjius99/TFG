1.- Se añade el inicio de sesion y el registro de usuarios de la APP https://www.youtube.com/watch?v=1XQJ_zRIBPU
2.- Se realiza la conexion a la red Ethereum
3.- Cuando se inicia el servidor, con una cuenta de las default se lanza el smart contract



CAMBIOS

-----------
addNews
-----------
'''try:
    if addNews(contract_address, abi, request.user.wallet, title, request.user.org_name, private_key):
        news = searchNews_byName(contract_address, abi, title)
        author = UserProfile.objects.get(org_name=request.user.org_name)
        news_model = News(newsId=news[2], title=title, content=contenido, author=author, legitima=news[5])
        news_model.save()
        if (author.reputation != 5):
            author.reputation = author.reputation + 1
        author.save()
        messages.success(request, 'Has añadido una noticia')
        return redirect('principal')
    else:
        messages.error(request, 'No se ha añadido la noticia')
        return redirect('principal')
except Exception as e:
    error = 'Ha ocurrido algún problema al añadir la noticia'
    return render(request, 'error.html', context={'error': error})'''
